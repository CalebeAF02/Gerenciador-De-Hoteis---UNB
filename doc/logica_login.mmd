sequenceDiagram
    participant A as Cliente (Gerente)
    participant CIG as Cntr Interface Gerente
    participant SA as Serviço Autenticável

database PG as Persistência Gerente

title Fluxo de Autenticação (Login - V2)

A->>CIG: requer Login
activate CIG

CIG->>CIG: solicita Email e Senha
CIG->>CIG: recebe Input

%% Criação e Validação dos Domínios
CIG->>CIG: Cria e Valida Domínios Email e Senha
alt Domínios Inválidos
CIG->>CIG: exibe "Erro de formato."
CIG-->>A: Retorna Erro
deactivate CIG
return
end

%% Delegação para a Camada de Serviço
CIG->>SA: autenticar(Email, Senha)
activate SA

%% Solicitação à Camada de Persistência
SA->>PG: autenticarGerente(email, senha)
activate PG

PG->>PG: Acesso ao Banco de Dados (Busca)

alt Credenciais Válidas
PG-->>SA: Retorna true (Gerente encontrado)
else Credenciais Inválidas
PG-->>SA: Retorna false
end
deactivate PG

%% Tratamento do Resultado no Serviço
alt Sucesso na Autenticação (true)
SA->>SA: atualiza estaLogado = true
SA-->>CIG: Retorna true (Logado com sucesso)
else Falha na Autenticação (false)
SA-->>CIG: Retorna false (Erro de autenticação)
end
deactivate SA

%% Exibição do Resultado no Controlador
alt Sucesso
CIG->>CIG: exibe "Login realizado com sucesso."
CIG->>CIG: exibe Menu Principal do Gerente
else Falha
CIG->>CIG: exibe "Erro: Email ou Senha inválidos."
end

CIG-->>A: Menu ou Mensagem de Erro
deactivate CIG
```eof

Este diagrama agora reflete sua arquitetura mais detalhada no formato Mermaid.

Qual o próximo diagrama que você gostaria de refatorar? Sugiro o **Fluxo de Criação de Hotel (CRUD - Criar)**.